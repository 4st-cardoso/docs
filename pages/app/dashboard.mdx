---
title: "Dashboard"
openapi: "GET /v1/app/{accountcode}"
---

<Tip>
  Se `accountcode` não for informado, a primeira conta do usuário é usada automaticamente.
  Use `start_date` e `end_date` para filtrar as métricas por período.
</Tip>
