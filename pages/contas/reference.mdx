---
title: "Contas"
description: "Criação e gerenciamento de contas na plataforma 4Selet Pay."
icon: "building-storefront"
---

## Visão Geral

As **Contas** representam lojas, empresas ou projetos cadastrados na plataforma. Cada conta possui seu próprio código identificador (`code`) que é utilizado no header `account` das requisições de cobrança.

---

## Campos de uma conta

| Campo            | Tipo   | Descrição                                     |
|------------------|--------|-----------------------------------------------|
| `id`             | integer| Identificador numérico da conta               |
| `code`           | string | Código único da conta (usado no header)       |
| `name`           | string | Nome da conta (loja/projeto)                  |
| `corporate_name` | string | Razão social                                  |
| `timezone`       | string | Fuso horário (ex: `America/Sao_Paulo`)        |
| `coin`           | string | Moeda padrão ISO 4217 (ex: `BRL`)             |
| `integration`    | object | Configurações de integração com gateways      |
| `settings`       | object | Configurações gerais da conta                 |

---

## Fusos horários

Para listar todos os fusos horários disponíveis, use:

```http
GET /v1/utils/time-zones
```

<Info>
  O código da conta (`code`) gerado é imutável e usado como identificador em todo o sistema.
  Guarde-o após a criação da conta.
</Info>
