---
title: "Consultar Cartão"
openapi: "GET /v1/clients/{clientCode}/cards/{cardId}"
---

<Note>
  Requer o header `account` com o código da conta. Retorna os dados de um cartão específico do cliente pelo seu `id` (UUID).
</Note>
