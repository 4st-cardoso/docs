---
title: "Login com Dados do Usuário"
openapi: "POST /v1/login/{app}"
---

<Note>
  Variante do login que retorna também os dados completos do perfil do usuário.
  Usar `app` como valor do parâmetro de path (ex: `POST /v1/login/app`).
</Note>
