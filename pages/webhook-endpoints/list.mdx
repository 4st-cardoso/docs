---
title: "Listar Endpoints"
openapi: "GET /v1/webhook-endpoints"
---

<Note>
  Requer o header `account` com o código da conta. Retorna todos os endpoints de webhook cadastrados para a conta, incluindo URL, eventos monitorados, status e datas.
</Note>
